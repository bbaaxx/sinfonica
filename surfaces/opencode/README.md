# Sinfonica OpenCode Surface

This package is the P1 skeleton for the OpenCode surface adapter.

## Scope

- Keep compatibility behavior in core unchanged during P1.
- Reserve this package for adapter-owned host glue in later phases.

## Layout

- `src/` adapter implementation modules
- `tests/` adapter-local tests
